#include "test.h"


void Test()
{
	/*
	int a = 1;
	int b = 2;
	int* p1 = &a;
	int* p2 = &b;
	Exchange(p1, p2);
	*/

	//PrintMulTable();
	
	//JudgeYear();
/*
	int arr[] = { 1, 2, 3, 4 };
	int len = 0;
	len = sizeof(arr) / sizeof(arr[0]) - 1;
	PrintArr(arr, len);
	Reverse(arr, len);
	PrintArr(arr, len);
	Empty(arr, len);
	Init_(arr,len);
	PrintArr(arr, len);
*/
	JudgePrime();

}



int main()
{
	Test();



	system("pause");
	return 0;
}